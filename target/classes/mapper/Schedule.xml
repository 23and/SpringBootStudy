<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boot.board.dao.ScheduleDao">
	<insert id="insertTitle" parameterType="boot.board.dto.ScheduleDto">
		INSERT INTO SCHEDULE (TITLE) 
		VALUE (#{title})
	</insert>
</mapper>
